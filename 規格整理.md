# 青青草原記帳本 - 專案規格書

## 1. 專案簡介
這是一個具備「家庭共享功能」的記帳應用程式，前端採用清新自然的「青青草原」風格。使用者可以紀錄個人收支，並透過家庭功能與家人共享帳務資訊。

## 2. 核心技術
*   **後端**: Spring Boot 3.x, Spring Data JPA
*   **資料庫**: MySQL / H2
*   **前端**: HTML5, CSS3 (原生), JavaScript (原生)
*   **通訊**: RESTful API

## 3. 功能模組

### 3.1 會員系統 (Member)
*   **登入機制**:
    *   **開發測試登入**: 輸入任意 Google ID 與暱稱即可登入。
    *   **Google 第三方登入**: 支援驗證 Google ID Token，並自動帶入使用者在 Google 上的暱稱與頭像。
*   **使用者資料**: 紀錄 Google ID、Email、暱稱、頭像 URL (`avatarUrl`)、註冊時間。
*   **個人化設定**:
    *   **更新暱稱**: 使用者可隨時修改在應用程式中顯示的暱稱。
    *   **更新頭像**: 使用者可上傳新圖片至圖床，並更新應用程式中顯示的頭像 URL。

### 3.2 帳戶管理 (Account)
*   **多帳戶支援**: 使用者可建立多個帳戶 (如：現金、銀行帳戶、私房錢)。
*   **餘額連動**: 記帳時自動扣減或增加對應帳戶的餘額。
*   **權限控管**: 只能查詢與操作屬於自己的帳戶。

### 3.3 記帳功能 (Record)
*   **收支紀錄**:
    *   支援 **支出 (EXPENSE)** 與 **收入 (INCOME)**。
    *   欄位：金額、日期、主分類、子分類、備註、關聯帳戶。
*   **動態分類系統**:
    *   **預設分類**: 系統內建常用分類 (食、衣、住、行、育、樂、金融、醫療、其他)。
    *   **自訂分類**: 使用者可手動輸入新的子分類。
    *   **歷史記憶**: 系統會自動將使用者曾經輸入過的子分類加入下拉選單中。
    *   **排序保證**: 下拉選單依照固定邏輯排序 (LinkedHashMap)。
*   **統計功能**:
    *   總收支統計 (總收入、總支出、結餘)。
    *   分類支出統計 (長條圖/圓餅圖資料來源)。

### 3.4 家庭共享 (Family)
*   **家庭管理**:
    *   **建立家庭**: 使用者可建立新家庭並成為家長 (Host)。
    *   **加入家庭**: 透過邀請碼申請加入現有家庭。
    *   **審核機制**: 家長可同意或拒絕加入申請。
    *   **查詢家庭資訊**: 成員可隨時查詢目前家庭的家長 (Host) 及邀請碼，方便邀請新成員。
*   **隱私與共享**:
    *   **家庭流水帳**: 家庭成員可查看全家人的公開流水帳 (不含私密細節)。
    *   **家庭統計**: 檢視全家人的分類支出總合。
    *   **隱私設定**: 成員可個別開關以下權限：
        *   `shareStats`: 是否公開個人總資產數據。
        *   `shareAccounts`: 是否公開詳細帳戶列表。

## 4. 資料結構 (簡述)

*   **Member**: `googleId` (PK), `nickname`, `shareStats`, `shareAccounts`
*   **Account**: `id`, `name`, `balance`, `member_id` (FK)
*   **Record**: `id`, `amount`, `type`, `category`, `subCategory`, `date`, `account_id` (FK)
*   **Family**: `id`, `name`, `inviteCode`, `host_id` (FK)
*   **FacilyJoinRequest**: `id`, `applicant_id`, `family_id`, `status` (PENDING/APPROVED/REJECTED)

## 5. 前端設計
*   **風格**: 淺綠色系、圓角卡片設計、自然舒適。
*   **互動**: 單頁應用 (SPA) 體驗，透過 JavaScript 切換 Tab 與 Modal。
*   **體驗優化**:
    *   登出後清除暫存。
    *   記帳時支援「新增子分類」與「取消新增」的切換。
    *   隱私設定變更即時同步。
