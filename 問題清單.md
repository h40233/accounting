# 開發過程問題清單與解決方案

本文件記錄了在開發「青青草原記帳本」過程中遇到的技術問題、原因分析以及最終的解決方案，供日後開發或使用其他 AI Agent 時參考。

## 1. PowerShell 指令語法錯誤
*   **問題**: 嘗試建立多個資料夾時，使用 `mkdir "path/a" "path/b"` 或 `&&` 語法導致 PowerShell 報錯。
*   **原因**: Windows PowerShell 的 `mkdir` (即 `New-Item`) 行為與 Linux Bash 不同，且 `&&` 在舊版 PowerShell 中不被支援。
*   **解決**: 使用分號 `;` 區隔指令。
    ```powershell
    mkdir "path/a"; mkdir "path/b"
    ```

## 2. 程式碼取代失敗 (Content Mismatch)
*   **問題**: 試圖修改 `RecordService.java` 的 `deleteRecord` 方法時，因提供的 `old_string` 與檔案實際內容 (空白、縮排或版本差異) 不完全匹配，導致工具替換失敗。
*   **教訓**:
    *   在執行 `replace` 前，務必先 `read_file` 確認最新的檔案內容。
    *   若無法精確匹配大段落，可嘗試縮小匹配範圍，或針對新增功能改用「附加在檔案末端」的策略。

## 3. 下拉選單 vs 手動輸入 (UX 衝突)
*   **問題**: 使用者希望分類有固定選項，但又想保有「自訂子分類」的彈性。
*   **解決**:
    *   **前端**: 採用「混合式 UI」。預設顯示 `<select>`，當選到特殊選項 `[+ 新增...]` 時切換為 `<input>`。
    *   **後端**: 實作 `getCategoryOptions` API。除了回傳寫死的預設分類外，還會動態掃描 DB 中的歷史紀錄，將使用者曾輸入過的自訂分類自動合併回選單中。

## 4. 下拉選單順序錯亂
*   **問題**: 使用 `HashMap` 儲存分類結構，導致前端顯示時順序隨機跳動 (例如：食、衣、住、行 變成 衣、行、食、住)。
*   **解決**: 後端改用 `LinkedHashMap` 來建構分類 Map，確保插入順序 (Insertion Order) 被保留。

## 5. 前端登出無效
*   **問題**: 點擊登出連結只觸發 `location.reload()`，頁面重整後 `localStorage` 中的 User Token 仍在，導致自動重新登入。
*   **解決**: 修改 `onclick` 事件，呼叫 `app.logout()` 明確執行 `localStorage.removeItem(...)` 後再重整。

## 6. 自訂子分類「無法反悔」
*   **問題**: 切換到「輸入框模式」後，沒有按鈕可以切換回「下拉選單模式」。
*   **解決**: 在輸入框旁新增一個「取消 (X)」按鈕，點擊後隱藏輸入框並重置下拉選單。

## 7. 隱私設定不同步
*   **問題**: 在家庭分頁更改隱私設定後，雖然資料庫已更新，但前端 `localStorage` 仍保留舊的設定值。登出再登入 (或重整) 後，Checkbox 狀態會跳回舊的。
*   **解決**:
    1.  **寫入時**: `updatePrivacy` 成功後，同步更新 `app.user` 物件並寫回 `localStorage`。
    2.  **讀取時**: `loadFamily` 載入時，強制依據 `app.user` 的最新狀態來設定 Checkbox 的勾選狀態。

## 8. API 文件缺乏具體範例
*   **問題**: 僅提供 API 網址與參數說明，缺乏實際的 JSON 格式，導致開發者（或接手的 AI）難以想像資料結構，容易產生誤解。
*   **解決**: 強制要求在 `API.md` 中為每一個 Endpoint 列出具體的 **Request Body Sample** 與 **Response Body Sample**，包含欄位型態與範例值。